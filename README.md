# Leads-Tracker
A Lead Tracking Chrome Extension that lets users save the active browser tab or custom URLs. The extension uses the Chrome Tabs API and localStorage to persist data, dynamically rendering saved leads with clickable links.
- Save current browser tab URL
- Add custom URLs manually
- Persistent storage using localStorage
- Clickable links that open in a new tab
- Clear all saved leads with one action
